'use client';

/**
 * components/layout/CustomCursor.tsx — STUB.
 * Plan 03-05 (Wave 3) implements the constrained tracer cursor.
 * IMPORTANT contract: native cursor must STAY visible (no takeover).
 */
export function CustomCursor() {
  return null;
}
