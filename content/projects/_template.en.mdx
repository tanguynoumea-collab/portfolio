---
slug: _template
title: Tech Project Template
year: 2025
category: tech
cover: /projects/_template/cover.jpg
summary: Reusable project case-study template — duplicate it, rename it, fill in the content.
featured: false
stack:
  - Next.js
  - TypeScript
  - Tailwind CSS
  - Motion
repo: https://github.com/tanguynoumea-collab/portfolio-template
liveUrl: https://example.com
# order (optional) — ascending sort key in the grid; projects without one sort last.
order: 99
# revit (optional, BIM tools) — targeted Revit version(s), e.g. "Revit 2024 · 2025".
# proprietary (optional) — true for a private/employer repo: shows a "Proprietary" badge instead of a dead repo link.
# gallery (optional) — uncomment and adapt to render an image gallery.
# Place the files in public/projects/[slug]/.
# gallery:
#   - /projects/_template/1.jpg
#   - /projects/_template/2.jpg
---

## Context

Describe the project context here (2-3 sentences): who it is for, why it matters, what was at stake.

## Challenge

Name the main problem to solve in one paragraph — technical, product, or creative.

## Process

Tell the story of the work (2-3 paragraphs). You may embed a `<Callout variant="info" title="…">…</Callout>` for an author's note, or a triple-backtick fenced code block for a Tech project.

## Outcome

Close with what shipped and a quantified or qualitative win where plausible.

---

Note: this file starts with an underscore, so `lib/projects.ts` filters it out (D-24) — it never appears in the project list or gallery. Aim for 250-400 words per locale on real entries; the template itself is excluded from the `scripts/check-mdx-structure.ts` gate. `category` is `bim` (BIM·Revit tools) or `tech` (other tools & apps). Fields: `stack[]` required; `revit?`, `repo?` (public repos only), `liveUrl?`, `proprietary?`, `order?` optional.
